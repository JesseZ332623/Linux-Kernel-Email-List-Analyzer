# LKML 分析报告持久化配置属性
app:
  analyze-report-persistence:
    # 报告需要保存到的本地路径
    report-path-prefix: "E:\\LKML-Analyze-Report"

    # 存储分析报告对象的桶名
    report-bucket-name: "lkml-analyze-reports"

    # 报告文件名最大长度
    max-file-name-len: 255