<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jesse.analyze_report_discuss.repository.AnalyzeReportDiscussSessionRepository">

    <!-- 删除一篇分析报告下指定的会话记录 -->
    <delete id="deleteBySessionId">
        DELETE FROM
            ai_analyze_discuss_session
        WHERE
            task_id        = #{taskId}
            AND
            session_id IN
            <foreach collection="sessionIds"
                     item="sessionId"
                     open="("
                     separator=","
                     close=")">
                #{sessionId}
            </foreach>
    </delete>

</mapper>